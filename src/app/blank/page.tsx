/**
 * Inspection lock landing — hech qanday UI / API / stack
 */
export default function BlankPage() {
  return (
    <div
      style={{
        position: "fixed",
        inset: 0,
        background: "#000",
        zIndex: 999999,
      }}
    />
  );
}
