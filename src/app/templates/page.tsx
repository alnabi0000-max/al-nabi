import { TemplatesExplorerClient } from "@/components/templates/TemplatesExplorerClient";

export const metadata = {
  title: "Al-Nabi Template Explorer",
  description: "500+ Al-Nabi video templates — one-click transfer to Studio",
};

export default function TemplatesPage() {
  return <TemplatesExplorerClient />;
}
