# Template previews

Optional local clips: `preview_180.mp4`, `preview_<id>.mp4`.

If a clip is missing, the UI tries one remote sample, then shows
`poster-placeholder.svg` and stops further network retries.
